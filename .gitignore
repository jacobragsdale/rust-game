/target
scores.db

# Saved games written by the pause menu (Q-3b). A save is a text file, and a
# readable one, but it is somebody's run rather than content.
/saves
.idea/

# Personal Claude Code permissions; the shared skill lives in .claude/skills/
.claude/settings.local.json

# Scratch git worktrees created for parallel agent runs
.claude/worktrees/
